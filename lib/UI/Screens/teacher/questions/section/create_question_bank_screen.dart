import 'package:flutter/material.dart';

class CreateQuestionBankScreen extends StatelessWidget {
  const CreateQuestionBankScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
